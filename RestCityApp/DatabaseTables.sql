CREATE TABLE [dbo].[Table]
(
	[Id] INT NOT NULL PRIMARY KEY,
    CityName varchar(50), 
    State VARCHAR ,
    Geographicregion varchar(50) ,
    country varchar(50) ,
     TouristTating varchar, 
     DateEstablished DateTime,
     EstimatedPopulation int,
     CountryInformation varchar
)
